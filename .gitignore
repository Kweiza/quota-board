# Build output
/target
/dist
/node_modules
/src-tauri/gen

# Private working artifacts: implementation plans, session handoffs, raw
# research logs. Never published. See AGENTS.md.
/.local/

# Agent session scratch. `.claude/` and `.flightdeck/` also hold git worktrees,
# which must never be committed into the repository they are checked out from.
/.superpowers/
/.claude/
/.flightdeck/
/.playwright-mcp/
